# ---------------------------------
# R / RStudio
# ---------------------------------
# (Your existing files - all correct)
.Rproj.user
.Rhistory
.RData
.Ruserdata

# ---------------------------------
# renv (Package Management)
# ---------------------------------
# Ignore the library itself, where packages are installed.
# We KEEP renv.lock, which is the recipe to re-install them.
renv/library/
renv/python/
renv/staging/

# ---------------------------------
# Project-Specific (Data & Output)
# ---------------------------------
# Ignore all data. Script 01 downloads it, so we don't need to
# track it in Git.
data/

# Ignore all outputs. Our scripts generate these plots and models,
# so we don't track the results, only the code.
output/

# ---------------------------------
# Python / Keras
# ---------------------------------
# Ignore Python cache files that Keras might create
__pycache__/
*.pyc

# ---------------------------------
# OS-specific files
# ---------------------------------
# Mac
.DS_Store
# Windows
Thumbs.db